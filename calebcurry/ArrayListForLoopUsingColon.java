package com.dawittsegay.calebcurry;

public class ArrayListForLoopUsingColon {

	public static void main(String[] args) {
		// for each loop in java
		List<Integer> grades = Arrays.asList(5, 4, 3, 2, 1);
		
		for(int grade : grades) {           /*the syntax for "for each" loop is -> data type + variable + : + then set them to 
											which the grades comes from*/
			System.out.println(grade);
			
			
			/*the above code is the same as the previous code i,e ,
			 /*import java.util.Arrays;
import java.util.List;

public class MySweetProgram25 {

	public static void main(String[] args) {
		
		//iterating through loops
		List<Integer> grades = Arrays.asList(5, 4, 3, 2, 1);
		//grades.size: returns number of elements in an array
		for (int i = 0; i < grades.size(); i++) {
			System.out.println(grades.get(i));			//here the grades.get(i) gets the value and then multiply it by two and then set it to an index (i)
		}

	}

}

			 * */
			
		}

	}

}
